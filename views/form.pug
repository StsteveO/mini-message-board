extends layout

block content
  h1= formTitle
  form(method= "POST", action= "/new") 
    p.form-design Message: 
        textarea(type="text", placeholder="Your Message", id="new_message", name="new_message", required, rows="4", cols="50", style="width:50%")
    p First Name:    
        input(type="text", placeholder="First Name", id="new_message", name="name", required)
    button(type="submit", value="Enter Message") Enter Message

  a(href= "./")
    button(type= "button") Return Home
